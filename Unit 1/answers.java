//solution code
      /*while(frontIsClear())
      {
         while(frontIsClear())
         {
            move();
            if(nextToABeeper())
               order();
         }
         if(nextToABeeper())
            order();
         turnRight();
         if(frontIsClear())
         {
            move();
            turnRight();
            if(nextToABeeper())
               order();
         }
         while(frontIsClear())
         {
            move();
            if(nextToABeeper())
               order();
         }
         if(nextToABeeper())
            order();
         turnLeft();
         if(frontIsClear())
         {
            move();
            turnLeft();
         }
         if(nextToABeeper())
            order();
      
      }
      while(!facingSouth())
         turnLeft();
      while(frontIsClear())
      {
         move();
      }
      turnRight();
      while(frontIsClear())
         move();
      
   */
   

 //solution code
      /*while(frontIsClear())
      {
         if(nextToABeeper())
            pickBeeper();
         move();
         turnRight();
         move();
         while(!facingNorth())
            turnLeft();
      }
      pickBeeper();
      explode();*/
